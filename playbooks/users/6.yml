---
- name: Verify that play throws error when remote_user_name is mentioned in Ldap user creation
  hosts: hmcs
  collections:
      - ibm.power_hmc
  connection: local

  tasks:
  - name: Create hmc user
    hmc_user:
      state: present
      hmc_host: "{{ inventory_hostname }}"
      name: hmcldap
      hmc_auth:
        username: hscroot
        password: abc123
      attributes:
        resourcerole: AllSystemResources
        authentication_type: ldap
        taskrole: hmcoperator
        remote_user_name: cla@AUSTIN.IBM.COM
    register: result

  - name: Output Users
    debug:
      msg: "{{ result }}"

